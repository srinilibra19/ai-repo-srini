# Story Journal: US-E0-002 — Self-signed mTLS certificate generation for local Solace

## Session 1 — 2026-03-22
Started: new story, no prior journal entry
Completed this session:
  - ST-01: generate-certs.sh — files: local-dev/certs/generate-certs.sh
    - Two security fixes applied after /security-reviewer:
      - server-combined.pem moved to chmod 600 (was incorrectly in chmod 644 block)
      - Passwords removed from stdout summary (replaced with non-revealing indicators)
  - ST-02: .gitignore for certs dir — files: local-dev/certs/.gitignore
  - ST-03: .env.example update — files: local-dev/.env.example
    - Added KEYSTORE_PASSWORD and TRUSTSTORE_PASSWORD variables with changeit defaults
  - ST-04: docker-compose.yml update — files: local-dev/docker-compose.yml
    - Added tls_servercertificate_filepath env var to Solace service
    - Added ./certs:/etc/solace/certs:ro bind mount

Decisions made:
  - Certs written directly to local-dev/certs/ (not a subdirectory) — matches existing
    local-dev/.gitignore patterns which already reference certs/ca.crt etc.
  - Passwords parsed from .env using grep/sed rather than sourcing the file — avoids
    executing arbitrary shell code from .env
  - server-combined.pem uses chmod 600 not 644 because it contains the server private key

Accepted deviations:
  - Low security finding accepted: ./certs bind mount exposes all cert files (including
    client.key, client-keystore.p12) to Solace container. Accepted because certs are
    local-dev only, self-signed, regenerated on demand, no production data.

Ended at: story complete — all ACs met, all sub-tasks done
