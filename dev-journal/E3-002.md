# Story Journal: US-E3-002 — Database schema migration — audit_messages table

## Session 1 — 2026-03-25
Started: beginning (E5-001 COMPLETE, E3-002 PENDING)

Completed this session:
  - ST-01: V1__create_audit_messages.sql — src/main/resources/db/migration/V1__create_audit_messages.sql
    - Code review FAIL: duplicate unique index (UNIQUE constraint already creates implicit index). Fixed.
    - Security review PASS.
  - ST-02: AuditMessage.java — src/main/java/com/middleware/hermes/model/entity/AuditMessage.java
    - Code review FAIL: createdAt/updatedAt lacked @Generated + @ColumnDefault — Hibernate would INSERT NULL
      into NOT NULL columns. Fixed with @ColumnDefault("now()") + @Generated(INSERT) / @Generated(INSERT,UPDATE).
    - Security review PASS.
  - ST-03: OutboxStatus.java — src/main/java/com/middleware/hermes/model/OutboxStatus.java
    - Code review FAIL: no test class (resolved by ST-05). Security review PASS.
  - ST-04: AuditMessageRepository.java — src/main/java/com/middleware/hermes/repository/AuditMessageRepository.java
    - Code review FAIL: @Modifying without @Transactional (runtime DataAccessException risk). Fixed.
    - Code review FAIL: @Modifying without clearAutomatically=true (stale cache bug). Fixed.
    - Security review PASS.
  - ST-05: AuditMessageRepositoryTest.java + OutboxStatusTest.java
    - src/test/java/com/middleware/hermes/unit/repository/AuditMessageRepositoryTest.java (9 tests)
    - src/test/java/com/middleware/hermes/unit/model/OutboxStatusTest.java (8 tests)
    - Both reviews PASS (0 findings).
  - ST-06: application.yml — baseline-on-migrate: true (AC requirement)

Decisions made:
  - Used @Generated(INSERT) + @ColumnDefault("now()") on createdAt — DB owns the value, Hibernate re-fetches
  - Used @Generated(INSERT, UPDATE) + @ColumnDefault("now()") on updatedAt — DB trigger owns updates
  - OutboxStatus stored as VARCHAR(50) name() value — no JPA enum converter needed; string status is explicit
  - AuditMessage.status stored as String (not enum) — audit lifecycle values differ from OutboxStatus
  - @DataJpaTest with @AutoConfigureTestDatabase(replace=NONE) + H2 MODE=PostgreSQL for repository tests
  - Flyway disabled for test profile; Hibernate ddl-auto=create-drop satisfies unit test schema needs

Ended at: COMPLETE — all 6 sub-tasks done, DoD checklist passed
