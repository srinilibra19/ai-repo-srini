# Story Journal: US-E3-003 — Database schema migration — outbox_messages table with LISTEN/NOTIFY trigger

## Session 1 — 2026-03-25
Started: beginning (E3-002 COMPLETE, E3-003 PENDING)

Completed this session:
  - ST-01: V2__create_outbox_messages.sql — src/main/resources/db/migration/V2__create_outbox_messages.sql
    - REQ-DB-011 columns + retry_count/error_message (added beyond spec to support OutboxStatus.FAILED/DLQ transitions)
    - UNIQUE constraint on deduplication_id
    - outbox_notify_trigger: FOR EACH ROW, AFTER INSERT, pg_notify('hermes_outbox_channel', 'new')
    - Code review FAIL: test coverage (deferred to ST-04). Security review PASS.
  - ST-02: OutboxMessage.java — src/main/java/com/middleware/hermes/model/entity/OutboxMessage.java
    - @Enumerated(EnumType.STRING) for OutboxStatus, @Builder.Default status=PENDING, retryCount=0
    - @Generated(INSERT) on createdAt — DB DEFAULT NOW() owns the value
    - Code review FAIL: test coverage (deferred to ST-04). Security review PASS.
  - ST-03: OutboxMessageRepository.java — src/main/java/com/middleware/hermes/repository/OutboxMessageRepository.java
    - findPendingForUpdate / findFailedForRetry: native queries with FOR UPDATE SKIP LOCKED
    - markPublished / markFailed / markDlq: JPQL @Modifying(clearAutomatically=true) @Transactional
    - countByStatus: @Transactional(readOnly=true)
    - Code review FAIL: unused snsMessageId parameter in markPublished. Fixed (removed parameter).
    - Security review PASS.
  - ST-04: OutboxMessageRepositoryTest.java — src/test/java/com/middleware/hermes/unit/repository/OutboxMessageRepositoryTest.java
    - 10 tests: save defaults, createdAt DB population, enum round-trip, duplicate deduplicationId,
      markPublished, markFailed retry increment, markDlq terminal state, countByStatus (2 cases),
      full PENDING→FAILED→DLQ lifecycle
    - FOR UPDATE SKIP LOCKED not tested here (H2 limitation) — documented for integration suite (US-E11-002)
    - Both reviews PASS.
  - ST-05: V3__create_indexes.sql — src/main/resources/db/migration/V3__create_indexes.sql
    - 6 supplementary partial indexes: sns_message_id, consumer_group+status, payload_size_bytes>204800,
      published_at, DLQ status, aggregate_id
    - Code review FAIL: integration test coverage deferred to US-E11-002. Security review PASS.

Decisions made:
  - Added retry_count and error_message columns beyond REQ-DB-011 spec — required for OutboxStatus.FAILED/DLQ transitions per E6-005 AC
  - markPublished removed snsMessageId parameter — outbox rows are transient; audit_messages is authoritative for sns_message_id
  - FOR UPDATE SKIP LOCKED queries not unit-tested (H2 incompatible) — Testcontainers PostgreSQL integration test deferred to US-E11-002
  - outbox_notify_trigger: FOR EACH ROW not FOR EACH STATEMENT — fires per insert as required by REQ-DB-012

Ended at: COMPLETE — all 5 sub-tasks done, DoD checklist passed
