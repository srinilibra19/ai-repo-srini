# Story Journal: US-E0-004 — Solace local queue and subscription provisioning

## Session 1 — 2026-03-22
Started: New story, beginning from scratch.
Completed this session:
  - ST-01: provision-queues.sh — files: local-dev/solace-init/provision-queues.sh
  - ST-02: docker-compose.yml hermes-solace-init service — files: local-dev/docker-compose.yml
  - ST-03: README.md SDKPerf documentation — files: README.md

Decisions made:
  - POSIX sh (not bash) chosen for curlimages/curl:8 Alpine compatibility
  - semp_post() helper written with HTTP code case statement for clean error handling
  - DMQ created before main queue so deadMsgQueue reference resolves at creation time
  - maxDeliveredUnackedMsgsPerFlow: 32 aligns with JCSMP sub-ack-window-size standard
  - hermes-solace-init one-shot service uses depends_on: service_healthy + restart: "no"
  - curlimages/curl:8 chosen (no extra tooling needed beyond curl for SEMPv2 REST calls)
  - Idempotency via grep -q '"ALREADY_EXISTS"' — matches Solace SEMPv2 string error code (not numeric)
  - SDKPerf section added to README with standard (1KB) and large (250KB >200KB threshold) commands

Accepted deviations:
  - HTTP plaintext to Solace SEMP API — Standard image does not expose SEMP over HTTPS on port 8080
  - admin:admin hardcoded in provision-queues.sh — canonical Solace Standard Docker default; local dev only

Ended at: Story complete. All 3 ACs verified.
