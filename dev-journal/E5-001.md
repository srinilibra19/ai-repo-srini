# Story Journal: US-E5-001 — Spring Boot project scaffold

## Session 1 — 2026-03-23

Started: New story, no prior journal entry.

Completed this session:
  - ST-01: pom.xml + owasp-suppressions.xml
    Files: pom.xml, owasp-suppressions.xml
    Code review FAILs fixed: missing spring-boot-starter-aop, OTel BOM not imported
    Security review FAILs fixed: OWASP Dependency-Check plugin added to integration-tests profile

Decisions made:
  - Spring Boot 3.5.3 (latest known 3.5.x patch)
  - AWS SDK v2 2.26.0 via BOM
  - Resilience4j 2.4.0 via BOM
  - Testcontainers 2.0.4 via BOM
  - OTel 1.60.0 via opentelemetry-bom (overrides Spring Boot BOM to pin CLAUDE.md version)
  - spring-boot-starter-aop explicit — required for Resilience4j annotation proxying
  - OWASP Dependency-Check bound to integration-tests profile (not default build)
  - skipTestScope=true — test JARs not in runtime image, no need to scan
  - Flyway 11.x requires flyway-database-postgresql module explicitly
  - H2 in test scope for unit-test context loads only
  - Testcontainers postgresql + localstack for integration tests

Ended at: ST-01 complete. Starting ST-02 (.gitignore) next.

## Session 2 — 2026-03-23 (continued)

Started: ST-02 complete from session 1. Continuing with ST-02 → ST-03.

Completed this session:
  - ST-02: .gitignore
    Files: .gitignore
    Code review FAILs fixed: added *.env pattern, removed .terraform.lock.hcl from exclusions,
      narrowed *.tfvars to *.auto.tfvars + *.secret.tfvars only
    Security review: PASS (0 findings)

  - ST-03: HermesApplication.java + HermesApplicationTest.java + application-test.yml
    Files: src/main/java/com/middleware/hermes/HermesApplication.java
           src/test/java/com/middleware/hermes/HermesApplicationTest.java
           src/test/resources/application-test.yml
    Code review: PASS (0 findings)
    Security review: PASS (2 LOW findings — not corrected per user decision)
      LOW-1: http://localhost:4566 in test config uses plaintext HTTP (LocalStack — intentional)
      LOW-2: access-key-id/secret-access-key hardcoded as 'test' without env var substitution

Decisions made:
  - @EnableScheduling on HermesApplication — required for outbox poller @Scheduled fallback
  - H2 MODE=PostgreSQL in test JDBC URL for compatibility with PostgreSQL-specific SQL
  - Flyway disabled in test profile — Hibernate create-drop handles schema for H2
  - Solace SolaceJavaAutoConfiguration excluded in test profile — no broker connection in unit tests
  - management.endpoints.enabled-by-default: false in test profile — no actuator needed in tests
  - Credential pattern for test profile: hardcoded 'test' stubs (user accepted LOW finding)

Ended at: ST-03 complete. Starting ST-04 (application.yml base config) next.

  - ST-04: application.yml (base config)
    Files: src/main/resources/application.yml
    Code review: PASS (0 findings)
    Security review: PASS (0 findings)

Decisions made (ST-04):
  - management.server.port: 8081 — actuator isolated from main port 8080
  - exposure.include: health,info,metrics,prometheus only — env/beans/heapdump excluded
  - spring.lifecycle.timeout-per-shutdown-phase: 55s — 5s headroom under terminationGracePeriodSeconds: 60
  - solace universal settings (reconnect-retries: -1, sub-ack-window-size: 32, ssl-validate-certificate: true) in base config — enforced across all profiles
  - hermes.* properties use ${ENV_VAR} with no defaults — explicit startup failure if missing
  - endpoint-override intentionally absent — only in local/test profiles for LocalStack
  - claim-check-threshold-bytes: 204800 — safe default, overridable per environment

Ended at: ST-04 complete. Starting ST-05 (Dockerfile) next.

  - ST-05: Dockerfile (multi-stage build)
    Files: Dockerfile
    Code review FAILs fixed:
      - Runtime base changed from ubi9/ubi-minimal + java-17-openjdk-headless
        to eclipse-temurin:17-jre-ubi9-minimal (satisfies CLAUDE.md: UBI9 + Temurin)
      - Added RUN chmod +x mvnw (defensive against Windows git core.fileMode=false)
      - Added microdnf install shadow-utils for groupadd/useradd on ubi9-minimal
    Security review: PASS (1 LOW — floating base image tags; resolved in buildspec-docker.yml per CLAUDE.md contradiction)

Decisions made (ST-05):
  - eclipse-temurin:17-jre-ubi9-minimal — satisfies both UBI9 (ROSA) and Temurin (CLAUDE.md) requirements
  - shadow-utils installed in runtime layer — required for groupadd/useradd on ubi9-minimal
  - hermes user UID/GID 1001 — consistent with OpenShift SCC non-root requirements
  - EXPOSE 8080 only — management port 8081 accessed internally by K8s probes via pod IP
  - -XX:MaxRAMPercentage=75.0 — 75% heap of cgroup memory limit
  - -XX:+ExitOnOutOfMemoryError — crash fast rather than thrash on OOM
  - Floating base image tags per CLAUDE.md Technology Stack; SHA pinning deferred to buildspec-docker.yml

Story complete: all 5 sub-tasks done.

Post-completion fixes discovered during ./mvnw test:
  - Testcontainers 2.x artifact IDs changed: junit-jupiter → testcontainers-junit-jupiter,
    postgresql → testcontainers-postgresql, localstack → testcontainers-localstack
  - solace-spring-boot-starter (wrong artifact ID) → solace-java-spring-boot-starter 5.2.0
    (correct JCSMP starter; the generic solace-spring-boot-starter tops out at 2.2.0)
  - Maven wrapper files generated: mvnw, mvnw.cmd, .mvn/wrapper/ (maven-wrapper.jar 3.3.2,
    Maven 3.9.14 distribution)

Final test result: Tests run: 1, Failures: 0, Errors: 0 — BUILD SUCCESS
