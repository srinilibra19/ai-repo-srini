# Story Journal: US-E0-003 — LocalStack initialisation for SNS FIFO and SQS FIFO

## Session 1 — 2026-03-22
Started: new story, no prior journal entry
Completed this session:
  - ST-01: bootstrap.sh — files: local-dev/localstack-init/bootstrap.sh
    - One code finding declined by user: multi-line JSON heredoc in CLI shorthand argument
      (python3 compaction approach) — accepted as-is, local dev only
    - One security Low declined (A02 HTTP plaintext — LocalStack community limitation)
    - One security Low declined (A07 local dev defaults in git-tracked script — consistent
      with .env.example accepted pattern)
  - ST-02: docker-compose.yml update — files: local-dev/docker-compose.yml
    - Added ./localstack-init:/etc/localstack/init/ready.d:ro bind mount to LocalStack service

Decisions made:
  - Used LocalStack 4.x native init script mechanism (/etc/localstack/init/ready.d/)
    rather than a separate one-shot Docker service — cleaner, no extra container
  - Secrets Manager delete-force-then-recreate pattern for idempotency
  - aws_cmd() wrapper function avoids repeating endpoint/region/format flags

Accepted deviations:
  - Multi-line JSON heredoc passed to AWS CLI --attributes shorthand (code FAIL declined)
  - Plaintext HTTP to LocalStack endpoint (LocalStack community limitation)
  - Local dev credential defaults in git-tracked script (consistent with .env.example pattern)

Ended at: story complete — all ACs met, all sub-tasks done
